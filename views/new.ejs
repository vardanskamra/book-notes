<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= act %> Review - My Book Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

    <div class="px-4 py-4 my-4 text-center">
        <h1 class="display-4 fw-bold text-body-emphasis">My Book Tracker</h1>
        <% if (act=="Edit"){ %>
            <h2 class="display-6 fw-bold text-body-emphasis">Edit Review</h1>
        <% } else{ %>
            <h2 class="display-6 fw-bold text-body-emphasis">Add a Review</h1>
        <% } %>
    </div>

    <div class="container my-5">
        <form class="row g-3 " action="<%= act === 'Edit' ? '/edit' : '/add' %>" method="post">
        <div class="col-md-6">
          <label for="title" class="form-label">Title</label>
          <input type="text" class="form-control" id="title" name="title" value="<%= act === 'Edit' ? book.title : '' %>">
        </div>

        <div class="col-md-6">
          <label for="author" class="form-label">Author</label>
          <input type="text" class="form-control" id="author" name="author" value="<%= act === 'Edit' ? book.author : '' %>">
        </div>

        <div class="col-12">
          <label for="review" class="form-label">Review</label>
          <textarea id="review" name="review" class="form-control" rows="10"><%= act === 'Edit' ? book.review : '' %></textarea>
        </div>

        <div class="col-md-6">
          <label for="date_read" class="form-label">Date Read</label>
          <input type="date" class="form-control" id="date_read" name="date_read" 
         value="<%= act === 'Edit' && book.date_read ? new Date(book.date_read).toISOString().split('T')[0] : '' %>">
        </div>

        <div class="col-md-4">
          <label for="rating" class="form-label">Rating</label>
          <select id="rating" class="form-select" name="rating">
            <option value="1" <%= act === 'Edit' && book.rating == 1 ? 'selected' : '' %>>1</option>
            <option value="2" <%= act === 'Edit' && book.rating == 2 ? 'selected' : '' %>>2</option>
            <option value="3" <%= act === 'Edit' && book.rating == 3 ? 'selected' : '' %>>3</option>
            <option value="4" <%= act === 'Edit' && book.rating == 4 ? 'selected' : '' %>>4</option>
            <option value="5" <%= act === 'Edit' && book.rating == 5 ? 'selected' : '' %>>5</option>
          </select>
        </div>

        <div class="col-md-2">
          <label for="isbn" class="form-label">ISBN</label>
          <input type="text" class="form-control" id="isbn" name="isbn" value="<%= act === 'Edit' ? book.isbn : '' %>">
        </div>

        <% if (act === 'Edit') { %>
            <input type="hidden" name="id" value="<%= book.id %>">
        <% } %>

        <div class="col-12 mt-5 d-flex justify-content-center">
            <button type="submit" class="btn btn-secondary">
                <%= act === 'Edit' ? 'Make Changes' : 'Add Review' %>
            </button>
        </div>
      </form>
    </div>
    
    <div class="container">
        <footer class="py-3 my-4">
          <ul class="nav justify-content-center border-bottom pb-3 mb-3">
            <li class="nav-item"><a href="https://github.com/vardanskamra" class="nav-link px-2 text-body-secondary">GitHub</a></li>
            <li class="nav-item"><a href="https://www.linkedin.com/in/vardan-s-kamra-837950260/" class="nav-link px-2 text-body-secondary">LinkedIn</a></li>
            <li class="nav-item"><a href="/" class="nav-link px-2 text-body-secondary">Home</a></li>
          </ul>
          <p class="text-center text-body-secondary">© 2024 Vardan S Kamra</p>
        </footer>
      </div>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    
</body>
</html>